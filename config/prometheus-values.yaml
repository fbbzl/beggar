grafana:
  adminPassword: grafana123
  replicas: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
prometheus:
  prometheusSpec:
    replicas: 1
    resources:
      requests:
        memory: "1Gi"
        cpu: "300m"
      limits:
        memory: "2Gi"
        cpu: "500m"
alertmanager:
  enabled: false
